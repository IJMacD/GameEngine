const UglifyJsPlugin = require('uglifyjs-webpack-plugin');
const config = require('./webpack.config.js');

if(!config.plugins) {
  config.plugins = [];
}

config.plugins.push(new UglifyJsPlugin());

config.output.filename = 'ijmacd-game-engine.min.js';

module.exports = config;
